export const metadata = {
  title: 'Offline - Luce İş Akışı',
  description: 'İnternet bağlantısı bekleniyor',
};

export default function OfflineLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
